import createHistory from 'history/createBrowserHistory';

// on calling createHistory function it returns a new history function, we then export  this returned history object to use it in other files;
export default createHistory();
